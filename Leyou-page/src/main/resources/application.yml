server:
  port: 8807
spring:
  application:
    name: page-service
  thymeleaf:
    cache: false

  rabbitmq:
      host: 192.168.210.130
      username: admin
      password: root
      virtual-host: /leyou    #在RabbitMQ中可以虚拟消息服务器VirtualHost，每个VirtualHost相当月一个相对独立的RabbitMQ服务器，每个VirtualHost之间是相互隔离的。exchange、queue、message不能互通。 相当于mysql的db。

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka
    registry-fetch-interval-seconds: 5
  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1


ly:
  thymeleaf:
    destPath: F:\Work\Java-Work\Reserch_LeYouMarket\Static_Themleaf

